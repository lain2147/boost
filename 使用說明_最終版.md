# ✅ 分布圖功能 - 使用說明

## 📦 版本資訊

此文件說明兩個版本的分布圖功能：

### 版本 1：從 TXT 解析版本（舊版）
**檔案名稱**：`分布圖功能_最終完整版.txt`
**版本日期**：2025-12-07
**總行數**：5,534 行（含錯誤 1004 修正）
**函數數量**：67 個（無重複）
**狀態**：✅ 可執行（已修正所有已知問題）

### 版本 2：從 Excel 讀取版本（新版，推薦）
**檔案名稱**：`DistributionChart_FromExcel_Complete.txt`
**版本日期**：2025-12-07
**主要函數**：`GenerateDistributionChartsFromExcel()`
**狀態**：✅ **推薦使用**（更穩定、無隱藏欄位）
**特點**：
- 從已生成的 Excel 測試報告讀取數據
- 兩欄式布局（參數 → 數據 → 圖表）
- Spec 標記顯示在數據表中
- 所有輸出為英文

---

## ⚠️ 重要提醒

### 請使用「最終完整版」，不要使用其他版本！

| 檔案名稱 | 狀態 | 說明 |
|---------|------|------|
| `分布圖功能_最終完整版.txt` | ✅ **請用這個** | 5,530 行，無重複函數 |
| `分布圖功能_超級完整版.txt` | ❌ 有問題 | 有重複函數定義錯誤 |
| `分布圖功能_修正版_可執行.txt` | ❌ 有問題 | 會出現「IsValidFieldValue 未定義」錯誤 |
| 其他舊版本 | ❌ 不要用 | 功能不完整或有錯誤 |

---

## 🔧 已修正的問題

### 1. VBA 編譯錯誤：「發現不確定的名稱: IsValidFieldValue」
- ✅ **原因**：函數定義重複（出現 2 次）
- ✅ **解決**：移除重複定義，保留第一個

### 2. 其他重複函數
- ✅ **CleanNumericValue**：重複 2 次 → 已移除重複
- ✅ **CleanFileName**：重複 2 次 → 已移除重複

### 3. 圖表生成問題（之前已修正）
- ✅ 圖表創建錯誤 → 使用儲存格錨點方式
- ✅ 參數表不見 → 正確放置在 F-G 欄
- ✅ Spec 線條不顯示 → 5 欄資料結構 + 智能判斷
- ✅ Y 軸小數顯示 → 強制整數格式

---

## 🚀 使用步驟

### 版本 1（舊版）：從 TXT 解析

#### 步驟 1：開啟 Excel VBA 編輯器

1. 開啟您的 Excel 檔案（`EXCEL_TO_TXT.xlsm`）
2. 按 `Alt + F11` 開啟 VBA 編輯器

#### 步驟 2：清除舊程式碼（重要！）

⚠️ **如果您之前有貼過其他版本的程式碼，請先全部刪除！**

1. 在左側專案樹找到您的模組
2. **全選舊程式碼**（Ctrl+A）
3. **刪除**（Delete）

#### 步驟 3：貼上程式碼

1. 開啟 `分布圖功能_最終完整版.txt`
2. **全選**（Ctrl+A）
3. **複製**（Ctrl+C）
4. 回到 VBA 編輯器
5. **貼上**（Ctrl+V）
6. **儲存**（Ctrl+S）

#### 步驟 4：執行程式

1. 關閉 VBA 編輯器（Alt+Q）
2. 在 Excel 中按 `Alt + F8`
3. 選擇 **`GenerateSEQDistributionCharts`**
4. 點選「執行」

#### 步驟 5：選擇檔案並生成

1. 選擇一個或多個 TXT 測試報告檔案
2. 等待處理（10-30 秒）
3. 選擇存檔位置
4. 完成！

---

### 版本 2（新版，推薦）：從 Excel 讀取

#### 步驟 1：準備 VBA 程式碼

1. 開啟任意 Excel 檔案（或 `EXCEL_TO_TXT.xlsm`）
2. 按 `Alt + F11` 開啟 VBA 編輯器
3. 插入新模組（Insert → Module）
4. 開啟 `DistributionChart_FromExcel_Complete.txt`
5. 全選複製（Ctrl+A → Ctrl+C）
6. 貼到 VBA 編輯器（Ctrl+V）
7. 儲存（Ctrl+S）

#### 步驟 2：執行程式

1. 關閉 VBA 編輯器（Alt+Q）
2. 在 Excel 中按 `Alt + F8`
3. 選擇 **`GenerateDistributionChartsFromExcel`**
4. 點選「執行」

#### 步驟 3：選擇已生成的測試報告

1. 在對話框中選擇已生成的 Excel 測試報告檔案（`.xlsx` 或 `.xlsm`）
2. 等待處理（5-20 秒）
3. 自動儲存並開啟結果檔案

#### 步驟 4：查看結果

1. 切換到「**Distribution Charts**」工作表
2. 查看所有 SEQ 的分布圖
3. 確認參數表、數據表、圖表都已正確生成

---

## 📊 生成結果

### 檔案名稱格式
```
{ModelName}_{Customer}_{Date}_{UnitCount}pcs_DistributionChart.xlsx
```

### 工作表內容（新版：從 Excel 讀取版本）

**布局說明**：左到右為 [參數表 2 欄] → [數據表 2 欄] → [圖表]

#### 單個圖表區塊布局

```
┌──────────────┬──────────────┬────────────────────────────┐
│ 參數表 (2欄) │ 數據表 (2欄) │ 圖表                       │
├──────────────┼──────────────┤                            │
│ Condition    │ VdcRead      │  VdcRead Distribution      │
│ Value        │ Frequency    │                            │
├──────────────┼──────────────┤  ┌──────────────────────┐  │
│ Vin      24  │ 4.55      1  │  │                      │  │
│ Fin       0  │ 4.63      1  │  │  █  █  █     ████    │  │
│ DelayTime 5.3│ 4.84      1  │  │                      │  │
│ LoadName  20 │ 5.2_Min      │  │  Y: Frequency        │  │
│ Mode     100 │ 5.28      1  │  │  X: Value            │  │
│ VdcMax   5.5 │ 5.28      1  │  │                      │  │
│ VdcMin   5.2 │ 5.29      4  │  │  (柱狀圖)            │  │
│ VppMax  0.05 │ 5.3       1  │  │                      │  │
│              │ 5.5_MAX      │  └──────────────────────┘  │
└──────────────┴──────────────┴────────────────────────────┘
  startCol      startCol+2     startCol+4
  (P-Q欄)       (R-S欄)        (T欄往右)
```

#### 完整工作表布局（Distribution Charts）

**重要原則**：
- **不同 SEQ 平行新增**：[SEQ.2] [SEQ.3] [SEQ.4] ... 水平排列
- **每個 SEQ 只顯示該 SEQ 的讀值**：不會混雜其他 SEQ 的數據

```
工作表橫向布局（從左到右）：

┌────────────────────┐  ┌────────────────────┐  ┌────────────────────┐
│ SEQ.2              │  │ SEQ.3              │  │ SEQ.4              │
│ Load Regulation    │  │ Input/Output       │  │ Turn On            │
├────────────────────┤  ├────────────────────┤  ├────────────────────┤
│ (VdcRead1)         │  │ (Iinrms)           │  │ (Ton)              │
│ 參數│數據│圖表     │  │ 參數│數據│圖表     │  │ 參數│數據│圖表     │
├────────────────────┤  ├────────────────────┤  ├────────────────────┤
│ (VdcRead2)         │  │ (Pin)              │  │                    │
│ 參數│數據│圖表     │  │ 參數│數據│圖表     │  │                    │
├────────────────────┤  ├────────────────────┤  └────────────────────┘
│ (VppRead1)         │  │ (Eff)              │
│ 參數│數據│圖表     │  │ 參數│數據│圖表     │
└────────────────────┘  └────────────────────┘

欄位配置範例：
SEQ.2: 欄 A-Z (第 1-28 欄)
  ├─ VdcRead1: A-D (參數2欄+數據2欄) + E往右 (圖表)
  ├─ VdcRead2: A-D (參數2欄+數據2欄) + E往右 (圖表)
  └─ VppRead1: A-D (參數2欄+數據2欄) + E往右 (圖表)

SEQ.3: 欄 AB-BZ (第 30-57 欄，間隔2欄)
  ├─ Iinrms: AB-AE (參數2欄+數據2欄) + AF往右 (圖表)
  ├─ Pin: AB-AE (參數2欄+數據2欄) + AF往右 (圖表)
  └─ Eff: AB-AE (參數2欄+數據2欄) + AF往右 (圖表)
```

**排列規則**：
- ✅ **同一個 SEQ 的讀值**：垂直排列（往下）
- ✅ **不同 SEQ**：水平排列（平行，往右）
- ✅ **每個 SEQ 獨立**：只顯示該 SEQ 的讀值，不會包含其他 SEQ 的數據
- ✅ **SEQ 間隔**：每個 SEQ 區塊間隔 2 欄，避免重疊
```

**重要特點**：
- **參數表**：Condition/Value 兩欄，Spec Max 紅色粗體，Spec Min 綠色粗體
- **數據表**：Reading/Frequency 兩欄，含 `_MAX` 和 `_Min` 標記（頻率為空）
- **圖表**：柱狀圖，Spec 位置柱高為 0（空白），Y 軸整數格式

---

## 🎨 支援的測試類型

| 測試類型 | 讀值數量 | 讀值名稱 |
|---------|---------|---------|
| **Load Regulation** | 11 | VdcRead1-3, VppRead1-3, VnRead1-3, dV21, dV31 |
| **Turn On** | 1 | Reading (Ton) |
| **Hold Up** | 2 | Tds, Tdl |
| **Short Circuit** | 1 | Pin |
| **Combine** | 6 | Vdc1-3, Vpp1-3 |
| **OLP** | 1 | Reading |
| **Dynamic** | 2 | Vs1, Vs2 |
| **Input/Output** | 最多 9 | Iinrms, Pin, Pdc, Eff, Pf, Idc, Vdc, Vpp, VinRead |

---

## ✨ 特色功能（新版：從 Excel 讀取）

### 1. Spec 標記顯示在數據表中
- **`_MAX` 標記（紅色粗體）**：Spec 上限值直接顯示在數據表中（例如：`5.5_MAX`）
- **`_Min` 標記（綠色粗體）**：Spec 下限值直接顯示在數據表中（例如：`5.2_Min`）
- **空白頻率**：Spec 位置的頻率欄位為空，圖表中對應柱高為 0
- **從小到大排序**：所有數值和 Spec 標記一起排序

### 2. Y 軸整數刻度
```vba
.MajorUnit = 1      ' 主要單位
.NumberFormat = "0" ' 整數格式（無小數）
.MinimumScale = 0   ' Y 軸最小值為 0
```

### 3. 兩欄式布局設計
- **參數表（2 欄）**：Condition/Value 格式，Spec 參數顏色標示
- **數據表（2 欄）**：Reading/Frequency 格式，含 Spec 標記
- **圖表區域**：450×280 像素柱狀圖
- **無隱藏欄位**：所有計算數據直接顯示在數據表中

### 4. 自動參數匹配
根據讀值名稱自動找到對應的 Spec 參數：
- `5.3_VdcRead1` → 基礎名稱 `Vdc` → `VdcMax`, `VdcMin`
- `12V_Pin` → 基礎名稱 `Pin` → `PinMax`, `PinMin`
- `Tds` → `TdsMax`, `TdsMin`
- `Eff` → `EffMin`（效率測試）

### 5. 智能四捨五入
```vba
Function RoundByValueRange(val As Double) As Double
    If Abs(val) >= 1 Then
        RoundByValueRange = Round(val, 2)  ' >= 1: 保留 2 位小數
    Else
        RoundByValueRange = Round(val, 3)  ' < 1: 保留 3 位小數
    End If
End Function
```

---

## 🆘 故障排除

### ❌ 錯誤：「發現不確定的名稱: IsValidFieldValue」

**原因**：您使用了舊版本檔案（有重複函數）

**解決**：
1. 確認您使用的是 `分布圖功能_最終完整版.txt`（5,530 行）
2. 刪除 VBA 中的舊程式碼
3. 重新貼上最終完整版

### ❌ 錯誤：執行階段錯誤 '1004'

**原因**：圖表物件刪除時發生錯誤（已在 v1.1 修正）

**解決**：確認使用最新版本（5,534 行），已包含安全的錯誤處理

### ❌ 無法識別 SEQ 區域

**原因**：Excel 工作表格式與預期不符

**解決**：
1. 確認 Excel 是由測試報告工具正確生成
2. 檢查是否有 SEQ 標題行（例如：`SEQ.2: Load Regulation Test`）
3. 確認參數表有 Condition/Value 欄位

### ❌ Spec 標記未正確顯示

**原因**：參數名稱不符合命名規則

**解決**：
1. 確認參數表中有對應的 Max/Min 參數（例如：`VdcMax`, `VdcMin`）
2. 檢查讀值名稱是否能正確提取基礎名稱
3. 參數名稱必須符合 `{BaseName}Max` 或 `{BaseName}Min` 格式

---

## 📝 檔案比較

| 特徵 | 修正版_可執行 | 超級完整版 | **最終完整版** |
|------|-------------|-----------|--------------|
| 總行數 | 6,611 | 5,604 | **5,530** |
| IsValidFieldValue | ❌ 未定義錯誤 | ❌ 重複定義 | ✅ 正確 |
| CleanNumericValue | ✅ | ❌ 重複定義 | ✅ 正確 |
| CleanFileName | ✅ | ❌ 重複定義 | ✅ 正確 |
| 圖表創建 | ❌ 有錯誤 | ✅ 已修正 | ✅ 已修正 |
| 參數表位置 | ❌ 有錯誤 | ✅ 已修正 | ✅ 已修正 |
| Spec 線條 | ❌ 不顯示 | ✅ 已修正 | ✅ 已修正 |
| **可用性** | ❌ 不可用 | ❌ 不可用 | ✅ **可用** |

---

## 🎯 版本歷史

### 版本 1（TXT 解析版本）

| 日期 | 版本 | 行數 | 狀態 |
|------|------|------|------|
| 2025-12-06 | 修正版_可執行 | 6,611 | ❌ IsValidFieldValue 未定義 |
| 2025-12-07 | 超級完整版 | 5,604 | ❌ 函數重複定義 |
| 2025-12-07 | 最終完整版 v1.0 | 5,530 | ✅ 移除重複函數 |
| 2025-12-07 | **最終完整版 v1.1** | **5,534** | ✅ **修正錯誤 1004** |

### 版本 2（Excel 讀取版本）

| 日期 | 版本 | 主要函數 | 狀態 |
|------|------|---------|------|
| 2025-12-07 | **從 Excel 讀取完整版** | `GenerateDistributionChartsFromExcel` | ✅ **推薦使用** |

**版本對比**：

| 特性 | 版本 1（TXT 解析） | 版本 2（Excel 讀取） |
|------|------------------|---------------------|
| 數據來源 | TXT 檔案 | Excel 工作表 |
| 解析方式 | 文字解析 | 儲存格讀取 |
| 布局 | 三區塊（隱藏+參數+圖表） | 兩欄設計（參數+數據+圖表） |
| 隱藏欄位 | A-E 欄隱藏計算數據 | 無隱藏欄位 |
| Spec 線條 | 圖表中的虛線 | 數據表中的標記 |
| 穩定性 | 較不穩定 | 穩定可靠 |
| 輸出語言 | 混合中英文 | 全英文 |

---

## ✅ 測試清單

### 版本 1（TXT 解析）測試項目

使用前請確認：

- [ ] 檔案名稱：`分布圖功能_最終完整版.txt`
- [ ] 檔案大小：5,534 行
- [ ] 無重複函數（67 個唯一函數）
- [ ] 包含 TXT 解析功能
- [ ] 包含分布圖生成功能
- [ ] 刪除舊版本程式碼
- [ ] VBA 啟用巨集

執行測試：

- [ ] 執行無錯誤
- [ ] 單一 TXT 檔案生成成功
- [ ] 多個 TXT 檔案合併成功
- [ ] A-E 欄自動隱藏
- [ ] F-G 欄參數表正確顯示
- [ ] H-O 欄圖表正確對齊
- [ ] Spec 線條智能顯示
- [ ] Y 軸整數刻度

---

### 版本 2（Excel 讀取）測試項目

使用前請確認：

- [ ] 檔案名稱：`DistributionChart_FromExcel_Complete.txt`
- [ ] 已有正確生成的 Excel 測試報告
- [ ] Excel 報告包含 SEQ 標題和 Condition/Value 參數表
- [ ] VBA 啟用巨集

執行測試：

- [ ] 正確識別所有 SEQ 區域
- [ ] 正確提取所有參數（Max/Min）
- [ ] 正確識別所有讀值欄位
- [ ] 正確提取讀值數據（排除空值、Maximum/Minimum）
- [ ] 正確計算頻率
- [ ] Spec Max/Min 正確加入數據集合
- [ ] 數據正確排序（從小到大）

顯示驗證：

- [ ] 參數表正確顯示（Max 紅色、Min 綠色）
- [ ] 數據表正確顯示（含 Spec 標記）
- [ ] Spec 位置頻率為空
- [ ] 圖表正確生成
- [ ] Spec 位置柱狀圖為空白
- [ ] Y 軸整數格式
- [ ] 顏色正確應用

布局驗證：

- [ ] 參數表在最左側（2 欄）
- [ ] 數據表在中間（2 欄）
- [ ] 圖表在最右側
- [ ] 同 SEQ 讀值垂直排列
- [ ] 不同 SEQ 水平排列
- [ ] 每個區塊間隔正確

---

## 💡 技術細節（新版：從 Excel 讀取）

### 核心實作要點

#### 1. 從 Excel 讀取數據

```vba
' 掃描 SEQ 區域
Function ScanAllSEQFromSheet(ws As Worksheet) As Collection
    ' 識別 SEQ 標題行（例如：SEQ.2: Load Regulation Test）
    ' 記錄 SEQ 起始行、結束行、類型
    ' 返回所有 SEQ 資訊的集合
End Function

' 提取參數
Function ExtractSEQParameters(ws As Worksheet, seqInfo As Object) As Object
    ' 在 SEQ 區域內尋找 Condition/Value 欄位
    ' 提取所有參數（Vin, Fin, VdcMax, VdcMin...）
    ' 返回參數字典
End Function
```

#### 2. 布局計算（兩欄式設計）

```vba
' 單個分布圖區塊的欄位配置
Sub CreateOneDistributionBlock(...)
    ' 參數表：startCol ~ startCol+1 (2 欄)
    Call WriteParameterTable(chartWs, params, readingName, startRow, startCol)

    ' 數據表：startCol+2 ~ startCol+3 (2 欄)
    Call WriteDataTable(chartWs, freqData, readingName, startRow, startCol + 2)

    ' 圖表：startCol+4 開始
    Call CreateDistributionChart(chartWs, freqData, readingName, startRow, startCol + 4)
End Sub
```

#### 3. Spec 匹配邏輯

```vba
' 提取基礎名稱
Function ExtractBaseName(readingName As String) As String
    ' "5.3_VdcRead1" → 去除前綴 "5.3_" → "VdcRead1"
    ' "VdcRead1" → 去除 "Read" → "Vdc1"
    ' "Vdc1" → 去除數字 → "Vdc"
    ExtractBaseName = "Vdc"
End Function

' 查找對應的 Spec 值
Function FindSpecValue(params As Object, baseName As String, specType As String) As Variant
    ' baseName = "Vdc", specType = "Max"
    ' searchKey = "VdcMax"
    If params.Exists(searchKey) Then
        FindSpecValue = params(searchKey)
    End If
End Function
```

#### 4. 頻率計算與 Spec 標記

```vba
Function CalculateFrequencyWithSpec(...) As Collection
    ' 1. 計算所有數值的頻率
    For Each val In values
        roundedVal = RoundByValueRange(CDbl(val))
        freqDict(roundedVal) = freqDict(roundedVal) + 1
    Next

    ' 2. 加入 Spec 標記（頻率為 Empty）
    If Not IsEmpty(specMax) Then
        allData.Add "5.5_MAX", Array("5.5_MAX", Empty, "max", RGB(255, 0, 0))
    End If
    If Not IsEmpty(specMin) Then
        allData.Add "5.2_Min", Array("5.2_Min", Empty, "min", RGB(0, 176, 80))
    End If

    ' 3. 從小到大排序（包含 Spec 標記）
    ' ... bubble sort implementation
End Function
```

---

## 📞 支援

如果您遇到任何問題：

1. **先確認使用的是「最終完整版」**（5,530 行）
2. **確認已刪除舊版本程式碼**
3. **查看本文件的故障排除章節**
4. 如仍有問題，請提供錯誤訊息截圖

---

**祝您使用愉快！**

---

## 📝 文件更新記錄

**最後更新**：2025-12-08
**文件版本**：v2.0（含兩個版本說明）

**版本狀態**：
- **版本 1（TXT 解析）**：v1.1 - ✅ 可用（已修正錯誤 1004）
- **版本 2（Excel 讀取）**：v1.0 - ✅ **推薦使用**（更穩定）

**主要更新**：
- 新增版本 2（從 Excel 讀取）的完整說明
- 更新布局設計為兩欄式（參數 → 數據 → 圖表）
- 新增 Spec 標記在數據表中的說明
- 新增版本對比表格
- 新增完整的測試清單
